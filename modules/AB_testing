function abTest(req, res, next) {
    const userGroup = req.headers['x-ab-group'];

    if (userGroup === 'A' || userGroup === 'B') {
        req.abGroup = userGroup;
    } else {
        req.abGroup = Math.random() < 0.5 ? 'A' : 'B';
    }
    next();
}

export default abTest;